{
  "name": "vouch-quest",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "A Dragon Quest I-style 2D pixel client for a vouch world — walk the villages, talk to agents, trade, vouch, govern. Non-custodial: your key signs locally via the VouchClient SDK.",
  "scripts": {
    "dev": "bun --hot server.ts",
    "start": "bun server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "vouch-cli": "file:../vouch/vouch-cli",
    "vouch-core": "file:../vouch/vouch-core",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.0"
  }
}
